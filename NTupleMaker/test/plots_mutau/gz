rm l.txt
ls *Nominal*root > n
while read line
do

p1=`echo $line | awk -F 'Nominal_' '{print $1}'`
p2=`echo $line | awk -F 'Nominal_' '{print $2}'`

mv $line $p1$p2
echo "$p1$p2" >> l.txt
done<n

rm ~/mutau.tar.gz
tar -cvf mutau.tar -T l.txt
gzip mutau.tar
mv mutau.tar.gz ~/.
echo "done..."
